[user]
  name = fschlegelone
  email = fabian@schlegel.one
[init]
  defaultBranch = main
[push]
  default = simple
  autoSetupRemote = true
[credential "https://dev.azure.com"]
  useHttpPath = true
[git]
	autoRefresh = true
	autoFetch = true
	refreshInterval = 10
  fetchInterval = 60
[merge]
  conflictstyle = diff3
[diff]
  compactionHeuristic = true
  colorMoved = default
[color]
	pager = no # disable pager colors to use nvim colors
[url "git@github.com:"]
	insteadOf = https://github.com/
[credential]
	helper = /usr/local/share/gcm-core/git-credential-manager
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
